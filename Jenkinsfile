pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo Build'
                sh 'env'
            }
        }
        stage('Test') {
            steps {
                sh 'echo Test'
                sh 'env'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo Deploy'
                sh 'env'
            }
        }
    }
}
